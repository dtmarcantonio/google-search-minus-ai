{
  "manifest_version": 2,
  "name": "google-search-minus-ai",
  "version": "1.0",
  "description": "Appends -ai to Google search queries to avoid AI results",
  
  "permissions": [
    "webRequest",
    "webRequestBlocking",
    "<all_urls>"
  ],
  
  "background": {
    "scripts": ["background.js"]
  }
}